[project]
name = "my-discord-bot"
version = "0.1.0"
description = "A personal discord bot project"
requires-python = ">=3.10"
dependencies = [
    "discord.py>=2.3.0",
    "python-dotenv>=1.0.0",
]

[project.scripts]
run-bot = "main:main"
